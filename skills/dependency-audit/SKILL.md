# Dependency Audit

When adding or upgrading dependencies:
- verify current supported versions
- inspect compatibility
- check license/security concerns when relevant
- minimize unnecessary dependencies
- run the project's dependency/build/test checks
