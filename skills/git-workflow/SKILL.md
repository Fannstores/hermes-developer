# Git Workflow

For repository work:
- inspect status and branch before editing
- make focused changes
- review diff before commit
- run tests before commit
- use descriptive commits
- never overwrite unrelated user work
- never claim a push succeeded without real command/API evidence

If a merge/rebase conflict occurs, inspect and resolve carefully rather than guessing.
