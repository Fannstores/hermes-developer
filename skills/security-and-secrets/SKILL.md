# Security & Secrets

Apply secure-by-default engineering.

Check:
- secrets are not committed
- environment variables are used for credentials
- logs do not expose tokens or account secrets
- external inputs are validated
- permissions are least-privilege
- network endpoints are authenticated where required
- dependency changes are reviewed

Never print or paste credentials into reports.
